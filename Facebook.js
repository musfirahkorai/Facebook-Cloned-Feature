// Get references to the "Add Friend" and "Remove" buttons
const addButton = document.getElementById("add");
const removeButton = document.getElementById("removee");

// Add an event listener to the "Add Friend" button
addButton.addEventListener("click", () => {
    // Add your logic here to handle adding a friend
    alert("Friend added!");
});

// Add an event listener to the "Remove" button
removeButton.addEventListener("click", () => {
    // Add your logic here to handle removing a friend
    alert("Friend removed!");
});
